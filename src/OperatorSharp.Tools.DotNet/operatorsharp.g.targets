<Project>
    <Target Name="_OperatorSharpGenerateCRDs" DependsOnTargets="Build">
        <Exec Command="dotnet operator-sharp generate-crds -a $(OutputPath)$(AssemblyName).dll -o $(OutputPath)crds" />
    </Target>
</Project>